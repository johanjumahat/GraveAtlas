# Adaptive Interface — Phase 16

**Status:** NOT IMPLEMENTED
**Date:** 2026-08-11

## What Phase 16 Requires
Task-oriented modes:
- RESEARCH MODE: AI + evidence + timeline + graph
- MAP MODE: map + geographic AI + historical layers
- ARCHIVE MODE: datasets + provenance + preservation
- INSTITUTION MODE: collections + verification + governance
- PUBLIC MODE: simple search + maps + cemetery exploration

## Current State
The app has a single mode with bottom navigation tabs (Home, Search, Map, Saved, Settings). No mode switching exists.

## Roadmap
- LATER: Adaptive modes require the underlying features (timelines, graph, AI map) to be built first
