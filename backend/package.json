{
  "name": "graveatlas-backend",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for GraveAtlas — community cemetery/grave database",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node tests/"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.122.0"
  }
}
